<span class="status-badge sb-@Kind">@Text</span>

@code {
    [Parameter] public string Text { get; set; } = "";
    [Parameter] public string Kind { get; set; } = "neutral"; // good | warn | bad | info | neutral
}
